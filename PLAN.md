# PLAN.md — Телеграм-бот для продажи курса

Статус: **фаза 2.5 завершена → переход в фазу 3 (тестирование и контент).** Все 7 шагов исправлений по ревью (`docs/CODE_REVIEW.md` → `docs/FIX_DESIGN.md`) реализованы и статически проверены (compileall=0, диф вычитан). Осталось: e2e-прогон вживую, реальный контент, деплой + миграция; плюс ручной Блок 0.

---

## Фаза 0 — Уточнение ТЗ ✅

Все вопросы закрыты. Решения зафиксированы в Журнале решений.

## Фаза 1 — Архитектура и стек ✅

- [x] Стек: Python (aiogram 3 + FastAPI), PostgreSQL, Docker, Caddy
- [x] Модель данных: 9 таблиц
- [x] Структура проекта: bot/admin/shared
- [x] Docker-compose, Dockerfile-ы
- [x] Caddy-конфиг

## Фаза 2 — Реализация ✅

- [x] Инфраструктура: Docker, shared-модули, config/database/models
- [x] Ядро бота: main.py, FSM, 10 хендлеров, клавиатуры
- [x] Группа поддержки: создание тем, пересылка
- [x] Рассылки: отправка из бота (polling БД)
- [x] Уведомления: назначение объекта → пользователю
- [x] Динамические ссылки из БД (юр-документы, бесплатные уроки)
- [x] SOCKS5 прокси: настройка через админку
- [x] Кэширование file_id для медиафайлов
- [x] Конвертация видео .mov → .mp4
- [x] Админка: дашборд, пользователи, рассылки, объекты, юр-документы, настройки
- [x] Русификация админки
- [x] Инлайн-меню (вместо reply-кнопок)

## Фаза 2.5 — Исправления по ревью ✅ (проект: `docs/FIX_DESIGN.md`)

**Все 7 шагов кода реализованы и проверены статически (2026-07-16). Остаётся Фаза 3 (e2e-прогон, реальный контент, деплой) и ручной Блок 0.**

Ручные шаги пользователя (Блок 0 — вне чекбоксов кода): отозвать токен, вычистить .env из git-истории, сменить пароли, `git rm --cached` мусора, description в BotFather, миграция SQL + перенос uploads при деплое. Плюс I6 (удаление мусорных медиафайлов) — вместе с git.

- [x] **Шаг 1 (Блоки A+B)** — безопасность и блокеры админки: порты на 127.0.0.1, create_all только в admin (бот ждёт БД `wait_for_db`), DSN quote_plus, fail-fast конфиг, фикс поиска (`cast(String)`, `@`, телефон), `Form(...)` в legal, `UPLOAD_DIR=/app/uploads`, санитайзинг загрузок (uuid-имя, whitelist, 20 МБ, pdf inline), `free_lessons_url` и `tariff_*_url` в настройках + helper `get_tariff_urls`/`get_setting` (`bot/app/services/app_settings.py`). Приёмка пройдена статически (py_compile=0, диф проверен). **B6-нюанс:** провязка воронковых клавиатур к tariff-url ОТЛОЖЕНА в Блок D (Шаг 3), чтобы не переделывать при рерайте клавиатур — сейчас только хранилище+helper. E2e-прогон админки — в Фазе 3.
- [x] **Шаг 2 (Блок C)** — согласие одной галочкой (один экран, три ссылки, кнопка «Принять», два ConsentLog), ужесточение ConsentMiddleware (без согласий — только /start и accept_all), отзыв согласия: снятие назначений объектов, сохранение журнала (telegram_id, user_id→NULL), удаление темы поддержки, после отзыва — консент-экран. Приёмка: без согласия бот не пускает никуда; отзыв не падает при назначенных объектах; журнал остаётся.
- [x] **Шаг 3 (Блок D)** — воронка по ТЗ: новая цель + персонализированные тексты (доп-фикс), отложенные отправки без sleep в хендлерах (`services/delayed.py: run_detached`), 30 сек перед «4 урока», 30-мин таймер отзывов (APScheduler `reviews_{tg_id}`) + идемпотентность (проверка STAGE_ORDER ≥ watched_lessons с коммитом до отправок), «Подробнее о курсе» по ТЗ (about → 15с → программа + «Узнать тарифы»), тарифы дословно (14 уроков!) с кнопками у каждого сообщения (`tariff_msg_kb`), «Выберите тариф» после таблицы (`tariff_final_kb`), /start и /menu + `main_menu`/`home_kb` → меню + state.clear, funnel_stage только вперёд (`advance_stage` везде), меню «Примеры работ» без задержек, меню «Отзывы» — только 2 отзыва, ЧаВо — одна кнопка, неизвестные команды → подсказка (`unknown_command`, StateFilter(None)). Приёмка пройдена статически: py_compile=0, диф вычитан оркестратором, grep на удалённые символы чист. E2e — Фаза 3.
- [x] **Шаг 4 (Блоки E+F, бот-часть)** — анкета объекта: кнопки Отмена/Далее/Отправить (`obj_cancel_kb`/`obj_skip_kb`/`obj_submit_kb`), F.text-фильтры + ловушка не-текста, сводка перед отправкой, частичное сохранение при отмене (`cancelled=True`, иначе не сохраняем), статус `accepted`, модель Object (`object_name` nullable + `cancelled` Boolean); вопросы/поддержка: единый `services/support_service.py` (ensure_thread с per-user asyncio.Lock, `deliver_to_support`/`deliver_text_to_support`, пересоздание закрытой темы 1 раз), «Задать вопрос» по ТЗ (ввод → подтверждение → Отправить, alert если пусто), единая автопересылка ВСЕХ типов через `copy_to` (StateFilter(None), private, кроме команд), ответы админов через `copy_message` + «⚠️ Не доставлено». Приёмка бот-side пройдена статически (compileall=0, диф вычитан). **E4 (админка: пометка «ПОЛЬЗОВАТЕЛЬ ОТМЕНИЛ» красным/жирным + `accepted`=«Принят») перенесена в Шаг 6 (Блок H) — вся админ-часть в одном месте.** E2e — Фаза 3.
- [x] **Шаг 5 (Блок G)** — рассылки: один механизм уведомлений (удалён `check_new_orders` из mailing.py и весь `services/notification.py`; `mailing_loop` без check_new_orders), `html.escape` на все поля + `timeout=5` в notifier, вызов уведомлений в `BackgroundTasks` (объект-роутер не висит на Telegram), таймзона МСК (`TZ` в config, ввод `datetime-local`→naive UTC, jinja-фильтр `msk` на все даты в 6 шаблонах + карточка-скачивание), ссылка на `mailing_task`, возобновление зависших `sending`→`pending` при старте бота, `asyncio.sleep(0.05)` + один повтор при `TelegramRetryAfter` (`_send_one`), статус `error` (sent==0 & failed>0) иначе `sent`, получатели собираются заранее (сессия не держится на отправках), логи батчем, счётчики «Доставлено/Ошибок» в списке (агрегат mailing_logs), `urlencode` в «Повторить», `onsubmit`-защита от двойного сабмита. Приёмка пройдена статически: compileall(bot/app+admin/app+shared)=0, диф вычитан, grep на удалённые символы/strftime чист. E2e — Фаза 3.
- [x] **Шаг 6 (Блок H + перенесённый E4)** — админка: `full_name`/`goal_ru`/`stage_ru` jinja-глобалы (чинит «Иван None» и падение скачивания карточки), datalist всех юзеров при назначении + ошибки через `?error=` (user_not_found/is_rejected/is_assigned), взаимоисключение статусов в POST (нельзя assign отклонённый / reject назначенный, эндпоинт `/reset` «Вернуть в Принят»), воронка на дашборде (кумулятивно по STAGE_ORDER + конверсия), дашборд «Ожидают назначения» считает `accepted`, русификация users.html/user_detail.html, пагинация 50/стр (users/objects/mailings), история mailing_logs в карточке юзера, `secrets.compare_digest` + `sleep(1)` + `samesite=lax` + lifespan вместо on_event; **E4: пометка `ПОЛЬЗОВАТЕЛЬ ОТМЕНИЛ` (красным жирным) в списке и карточке объектов при `cancelled`; `accepted`=«Принят» (E3), формы назначить/отклонить показываются при `accepted`.** Приёмка пройдена статически: compileall(admin/app)=0, все шаблоны парсятся, диф вычитан. **secure=True у cookie НЕ ставили (осознанно — локальное http-тестирование; в проде HTTPS через Caddy).** E2e — Фаза 3.
- [x] **Шаг 7 (Блок I)** — надёжность и чистка: I1 кэш file_id по пути+**mtime** с upsert (`ON CONFLICT`, +колонка `CachedFile.file_mtime`) — замена контента подхватывается без ручной чистки, гонка холодного кэша погашена; I2 `get_or_create_user` обновляет username/имя при изменении + гасит гонку INSERT (`IntegrityError`→rollback→reselect); I3 удалён мёртвый код (`SECRET_KEY`, `get_current_user`, `import secrets` в dependencies.py; `welcome_kb`/`tariffs_kb`/`learn_tariffs`/`notification.py` удалены ранее); I5 `docs/ARCHITECTURE.md` переписан по факту (оркестратором). Приёмка пройдена статически: compileall=0, диф вычитан. **I6 (удаление мусорных медиа `ждун.jpg`/`бабка на рынке.jpg`/*.mov/`.mimocode`) и I4 (MemoryStorage — осознанно оставлен) — оставлены пользователю (git + риск удалить используемое).**

## Фаза 3 — Тестирование и запуск

- [ ] Тестирование воронки end-to-end
- [ ] Загрузка реальных юр-документов
- [ ] Замена заглушек GetCourse (ссылки — через настройки админки)
- [ ] Загрузка реальных видео/изображений (кэш по mtime подхватит сам)
- [ ] Деплой на сервер (выполнить `docs/migrations/2026-07-16_fixes.sql`, перенести uploads, проверить порты)

---

## Журнал решений

- **2025-07-15 — Стек**: Python + aiogram 3 (бот), FastAPI + Jinja2 + SQLAlchemy (админка), PostgreSQL (БД), Docker, Caddy.
- **2025-07-15 — Порты**: бот — фоновый процесс, админка — 5000, PostgreSQL — 5433.
- **2025-07-15 — Прокси**: SOCKS5 через aiohttp-socks, timeout=300 (int, не ClientTimeout).
- **2025-07-15 — Кэш**: file_id в таблице cached_files, автоматическое заполнение при первой отправке.
- **2025-07-15 — Видео**: .mov → .mp4 через ffmpeg, .mov в .gitignore.
- **2025-07-15 — Меню**: инлайн-кнопки (InlineKeyboardMarkup), не reply-кнопки.
- **2025-07-15 — Юр-документы**: загружаются через админку, отдаются по публичному URL.
- **2025-07-15 — Оплата**: на стороне GetCourse, бот только фиксирует.
- **2026-07-16 — Ревью**: пять проходов, результаты в `docs/CODE_REVIEW.md`; проект исправлений — `docs/FIX_DESIGN.md`.
- **2026-07-16 — Уроков 14, не 13** (подтверждено заказчиком): в тарифе «Самостоятельный» писать «4 модуля, 14 уроков» — противоречие в ТЗ снято.
- **2026-07-16 — Согласие одной галочкой** (заказчик): один экран со ссылками на оферту, политику конфиденциальности и политику ПДн + одна кнопка «Принять»; в БД по-прежнему оба флага и два ConsentLog.
- **2026-07-16 — Журнал согласий хранить** после отзыва (152-ФЗ): consent_log.user_id → nullable + колонка telegram_id; при отзыве записи обезличиваются, добавляется запись об отзыве.
- **2026-07-16 — Меню «Отзывы» строго по ТЗ**: два отзыва + «Главное меню»; продающая цепочка — только в воронке.
- **2026-07-16 — Телефон**: поиск по полю phone в админке делаем, сбор телефона в боте — нет (лишний барьер в воронке).
- **2026-07-16 — Бесплатные уроки — одна внешняя ссылка**: ключ `free_lessons_url` в settings, поле в админке; загрузка файлом для этого типа убирается из UI.
- **2026-07-16 — Персонализация цели**: после нажатия кнопки цели бот шлёт персональный текст (тексты заказчика в CODE_REVIEW.md «Доп фиксы»), затем продолжает цепочку примеров.
- **2026-07-16 — FSM остаётся в MemoryStorage** (осознанно): сценарии короткие, /start и /menu чистят state; Redis не вводим.
- **2026-07-16 — Отложенные отправки**: не sleep в хендлерах, а detached-задачи (`run_detached`) без удержания сессии БД; 30-мин таймер отзывов — APScheduler (MemoryJobStore, рестарт не переживает — допущение).
- **2026-07-16 — Уведомления о назначении**: единственный механизм — `shared/notifier.py` из админки; `check_new_orders` и `services/notification.py` удаляются.
- **2026-07-16 — Кэш file_id**: ключ путь+mtime, запись через upsert — замена контента подхватывается без ручной чистки.
- **2026-07-16 — Статус новых заказов** — `accepted` («Принят»), как в ТЗ; `pending` не используется.
