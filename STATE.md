# STATE.md — состояние работы (обновляется каждой сессией)

## Текущая точка

- **2025-07-15. Фаза 2 — основной код готов.** Бот: 17 файлов (ядро, 10 хендлеров, FSM, клавиатуры, middleware). Админка: FastAPI + 6 роутеров + 8 шаблонов (дашборд, пользователи с поиском, рассылки, объекты, юр-документы). Shared: config, database, models (8 таблиц). Docker: docker-compose.yml, 2 Dockerfile-а, requirements.txt.

## Следующий шаг

- Тестирование: проверить что бот запускается, что хендлеры не конфликтуют, что БД создаётся корректно
- Деплой на сервер: запустить docker-compose, настроить Caddy, проверить работу
- Дошлифовка: заглушки на ссылки (юр-документы, GetCourse, бесплатные уроки) заменить на реальные

## Нюансы (грабли, лимиты, особенности — пополняется по ходу)

- Обвязка переносимая: пути нигде не захардкожены (хук берёт корень из payload `cwd` / `$env:CLAUDE_PROJECT_DIR`).
- Stop-хук блокирует завершение ответа, если файлы проекта изменены позже STATE.md — держать STATE.md свежим после каждого цикла.
- Порты не использовать: 3000-4000 и 8000-9000 (заняты другими проектами на сервере).
- Домен `andrew-bot-adminka.mak-o.ru` уже настроен в DNS и смотрит на сервер.
- Caddy уже установлен и работает на сервере для других проектов — добавить только конфиг для этого.
- Файлы в `files/` — тестовые. Реальные будут загружаться через админку.
- Токен бота и chat_id в `.env` — не коммитить.
- Ссылки на юр-документы, GetCourse, бесплатные уроки — пока заглушки (#offer, #privacy, #personal_data, #free_lessons, getcourse.example.com).
- Видео/файлы с кириллическими именами — InputFile в aiogram обрабатывает корректно.
- Админка: авторизация по cookie (пароль в ADMIN_PASSWORD). Сессия хранится в app.state.
- Рассылки: админка только создаёт запись. Отправка — задача для бота (polling БД). Реализовать позже.
