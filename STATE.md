# STATE.md — состояние работы (обновляется каждой сессией)

## Текущая точка

- **2026-07-16. ФАЗА 3 (деплой, пользователь). БАГ-ФИКС при e2e.** После отзыва согласия `revoke_confirm` удаляет запись User целиком, а `accept_all` (start.py) при отсутствии юзера делал `if not user: return` — тихий выход, «Принять» не реагировало (в логах Duration 4-8ms). Исправлено: `accept_all` теперь пересоздаёт User при отсутствии (как `get_or_create_user`, с `IntegrityError`→rollback→reselect). Ручной обход без деплоя — `/start` пересоздаёт юзера. compile=0. Выкат: `git pull && docker compose up -d --build bot`.

- **2026-07-16. ФАЗА 2.5 ЗАВЕРШЕНА (все 7 шагов кода). Шаг 7 (Блок I — надёжность/чистка) реализован и ПРОВЕРЕН.** Сабагент coder: `shared/models.py` (+`CachedFile.file_mtime` Float, +импорт Float); `bot/app/services/cache.py` (рерайт: кэш по пути+mtime, инвалидация при смене mtime, upsert `ON CONFLICT DO UPDATE` — гонка холодного кэша погашена); `bot/app/handlers/start.py` (`get_or_create_user`: обновление username/имени при изменении + `IntegrityError`→rollback→reselect); `admin/app/dependencies.py` (удалён мёртвый `SECRET_KEY`/`get_current_user`/`import secrets`). Оркестратор переписал `docs/ARCHITECTURE.md` по факту (I5). compileall(bot/app+admin/app+shared)=0, диф вычитан. **I6 (мусорные медиа) и I4 (MemoryStorage оставлен осознанно) — на пользователе.** Миграция `docs/migrations/2026-07-16_fixes.sql` содержит `file_mtime` (строка 8) — накатить на прод перед деплоем. Код НЕ закоммичен.

- **2026-07-16. Шаг 6 Фазы 2.5 (Блок H + E3/E4 — админка) реализован и ПРОВЕРЕН.** Сабагент coder: `admin/app/dependencies.py` (jinja-глобалы `full_name`/`goal_ru`/`stage_ru`); `dashboard.py` («Ожидают» теперь считает `accepted`; кумулятивная воронка по STAGE_ORDER + конверсия → `funnel`); `users.py`/`mailings.py` (пагинация 50/стр); `objects.py` (рерайт: datalist всех юзеров, ошибки `?error=`, взаимоисключение статусов, эндпоинт `/reset`, пагинация, `full_name` в выгрузке, дата в МСК); `auth.py` (`compare_digest`, `sleep(1)`, `samesite=lax`); `admin/app/main.py` (lifespan вместо on_event); шаблоны dashboard/users/objects/object_detail/user_detail/mailings (воронка, русификация, пагинация, пометка отмены, история рассылок, формы assign/reject/reset по статусу). Оркестратор вычитал диф, compileall=0, все шаблоны парсятся. Код НЕ закоммичен. **Важно: формы «Назначить/Отклонить» теперь показываются при статусе `accepted` (новые объекты приходят accepted, не pending) — до фикса были недоступны.**

- **2026-07-16. Шаг 5 Фазы 2.5 (Блок G — рассылки/уведомления) реализован и ПРОВЕРЕН.** Сабагент coder: удалён `bot/app/services/notification.py`; `bot/app/services/mailing.py` (рерайт `process_mailings`: получатели заранее под короткой сессией, `_send_one` с `TelegramRetryAfter`+1 повтор и `sleep(0.05)`, логи батчем, статус `error`/`sent`; `check_new_orders` вырезан); `bot/app/main.py` (без check_new_orders; reset `sending`→`pending` перед polling; `mailing_task=create_task`); `shared/notifier.py` (`html.escape` на все поля, `timeout=5`); `admin/app/routers/objects.py` (`BackgroundTasks` в assign, примитивы извлечены до add_task; download-дата в МСК); `shared/config.py` (`TZ=ZoneInfo("Europe/Moscow")`); `admin/app/dependencies.py` (jinja-фильтр `msk`); `admin/app/routers/mailings.py` (`datetime-local`→МСК→naive UTC; `urlencode` в repeat; `stats` доставок); шаблоны — все даты через `| msk` (mailings/user_detail/object_detail/legal/users/objects), +столбец «Доставлено/Ошибок», +`onsubmit`-защита. Оркестратор вычитал диф, compileall=0, grep на check_new_orders/notification/strftime чист. Код НЕ закоммичен.

- **2026-07-16. Шаг 4 Фазы 2.5 (Блоки E+F, бот-часть) реализован и ПРОВЕРЕН.** Сабагент coder: `shared/models.py` (Object.object_name→nullable, +`cancelled` Boolean), `bot/app/states.py` (+QuestionState.confirm, +ObjectState.waiting_confirm), `bot/app/keyboards.py` (question_kb с «Отправить», +obj_cancel_kb/obj_skip_kb/obj_submit_kb), `bot/app/handlers/object.py` (полный рерайт: обязательные шаги — только «Отмена»; фото/видео — «Далее»/skip-слова; сводка → «Отправить»/«Отмена»; не-текст на текстовом шаге → просьба текст; отмена → частичное сохранение cancelled=True если введено хоть что-то; отправка → status=accepted, cancelled=False, advance_stage object_submitted), новый `bot/app/services/support_service.py` (ensure_thread с per-user asyncio.Lock + refresh против гонки тем; `deliver_to_support` через copy_to; `deliver_text_to_support`; пересоздание темы 1 раз при thread_gone), `bot/app/handlers/question.py` (рерайт: ask→ввод→подтверждение→question_send; единая ловушка auto_forward StateFilter(None)+private для ЛЮБЫХ типов через copy_to; команды не трогает), `bot/app/handlers/support.py` (ответы админов через copy_message для любого типа + «⚠️ Не доставлено»; эхо-петля отсекается по from_user==bot). Оркестратор вычитал диф, compileall(bot/app+shared)=0, grep на старые символы чист. **E4 (пометка отмены в админке) и E3-русификация — перенесены в Шаг 6.** Код НЕ закоммичен.

- **2026-07-16. Шаг 3 Фазы 2.5 (Блок D — воронка) реализован и ПРОВЕРЕН.** Сабагент coder переписал 7 файлов + создал `bot/app/services/delayed.py` (`run_detached` — detached-таск с защитой от GC). Оркестратор вычитал диф глазами, py_compile=0, grep на удалённые символы (tariffs_kb/send_reviews/send_examples/enroll/…) чист. Ключевое: **отложенные отправки** через `run_detached` (сессия хендлера не держится на sleep'ах; callback.answer до задержек); **цель** — новый текст «🎯 Какая у вас цель?» + 3 персонализированных ответа (`GOAL_RESPONSES` в start.py); **воронка примеров** — интро сразу, видео 5/20/20с и «4 урока» через 30с в detached (`send_funnel_examples`); **отзывы** — APScheduler-job `reviews_{tg_id}` на now+30мин ИЛИ кнопка «Посмотрел» (job снимается), `send_funnel_reviews` идемпотентна (STAGE_ORDER-проверка + коммит watched_lessons ДО отправок); **«Подробнее о курсе»** (`course_detail`) → ABOUT → 15с → PROGRAM + «Узнать тарифы»; **тарифы дословно из ТЗ (14 уроков!)** в `_send_tariffs`, у каждого сообщения `tariff_msg_kb` (Купить X ₽ + Задать вопрос), затем таблица, затем «Выберите тариф» + `tariff_final_kb`; url тарифов из настроек (`get_tariff_urls`); **навигация** — `main_menu`/`cmd_start`/`cmd_menu` чистят state, `funnel_stage` только вперёд (`advance_stage` везде), неизвестные команды → подсказка (`unknown_command`, StateFilter(None), private); **меню** «Примеры работ» без задержек (`send_menu_examples`), «Отзывы» — 2 отзыва + домой (`send_menu_reviews`), ЧаВо — одна кнопка (`home_kb`). Код НЕ закоммичен (коммитит пользователь).

- **2026-07-16. Шаг 2 Фазы 2.5 (Блок C — согласие) реализован и ПРОВЕРЕН.** `shared/funnel.py` (новый, `advance_stage`), `shared/models.py` (ConsentLog.user_id nullable + telegram_id — синхронно с миграцией), `bot/app/keyboards.py` (consent_kb/consent_done_kb/home_kb вместо 4 старых consent-kb + welcome_kb), `bot/app/handlers/start.py` (один экран согласия + accept_all, cmd_start со state.clear и без повтора интро, команда /menu), `bot/app/middlewares/consent.py` (session из data, анти-спам 15с, /start@bot, ленивые импорты — циклов нет), `bot/app/handlers/consent.py` (отзыв: снятие назначений объектов через UPDATE, обезличенный журнал вместо удаления, удаление темы поддержки, после отзыва — консент-экран). py_compile=0, диф вычитан. Воронковые хендлеры start.py оставлены дословно под Блок D.

- **2026-07-16 (ещё позже). Реализован и проверен Шаг 1 Фазы 2.5 (Блоки A+B).** Сабагент coder внёс правки в 12 файлов + создал 2 новых (`bot/app/services/app_settings.py`, `docs/migrations/2026-07-16_fixes.sql`). Оркестратор проверил диф глазами: порты на 127.0.0.1; бот больше не делает `create_all` — ждёт БД через `wait_for_db()` (retry 60с), таблицы создаёт только admin; DSN с `quote_plus`; fail-fast конфиг (TELEGRAM_BOT_TOKEN/CHAT_ID в config.py, ADMINKA_URL в bot/main.py); поиск в админке починен (`cast(String)` вместо `cast(str)` — тот самый 500, + срез `@`, + телефон); загрузка юр-доков — `Form(...)`, uuid-имя, whitelist расширений, лимит 20 МБ, pdf отдаётся `inline`; `UPLOAD_DIR=/app/uploads` (volume); настройки `free_lessons_url`/`tariff_*_url` + поля в админке + helper'ы `get_setting`/`get_tariff_urls`. py_compile=0. **B6-нюанс:** провязка воронковых клавиатур к tariff-url отложена в Блок D (Шаг 3). Код НЕ закоммичен (коммитит пользователь). E2e админки — Фаза 3.

- **2026-07-16 (позже). Спроектированы исправления всех багов и расхождений из ревью.** Полный проект — `docs/FIX_DESIGN.md` (блоки A–I + Блок 0 ручных шагов пользователя + SQL-миграция). В `PLAN.md` добавлена Фаза 2.5 с 7 шагами реализации и критериями приёмки; решения заказчика и оркестратора — в Журнале решений. Ключевые решения заказчика: уроков **14** (не 13), согласие **одной галочкой** (один экран, три ссылки, одна кнопка), журнал согласий **хранить** после отзыва, меню «Отзывы» строго по ТЗ, телефон в поиске оставить (сбор не делать), бесплатные уроки — одна внешняя ссылка через настройки админки. Плюс доп-фикс заказчика: новый текст «🎯 Какая у вас цель?», новые подписи кнопок и персонализированные ответы на каждую цель (тексты — в CODE_REVIEW.md «Доп фиксы»). Код ещё НЕ правился — только проектирование.

- **2026-07-16 (ранее). Проведено полное ревью кода + сверка с ТЗ (пять проходов, ревью исчерпано).** Пятый проход: закрытая тема поддержки навсегда отрезает юзера (thread_id не сбрасывается); замена заглушек на реальный контент не подхватится из-за cached_files (добавлено в приоритеты!); гонка create_all при первом деплое; DB_PASSWORD в DSN без экранирования; гонки создания тем/кэша; статистика доставки рассылок не отображается. Четвёртый проход: `create_task(mailing_loop)` без ссылки (GC может убить цикл рассылок), funnel_stage перезаписывается назад (аналитика недостоверна), юр-доки отдаются скачиванием вместо просмотра, даты в админке в UTC, русификация users.html неполная, GetCourse-ссылки захардкожены. Снято одно замечание (предупреждение о рестарте прокси в UI есть). Третий проход: PostgreSQL (5433) и админка (5000) в docker-compose открыты на 0.0.0.0 — доступны из интернета (критично); `callback.answer()` после 45-сек задержек в «Примеры работ»/«Отзывы» падает («query is too old»); «бесплатные уроки» нельзя задать внешней ссылкой (только файл) — Т21; MemoryStorage теряет FSM при рестарте; ARCHITECTURE.md устарел. Вторым проходом добавлены Т12–Т20: пауза 3 сек вместо 30 в воронке, нет кнопки «Отправить» и текста «ответ придёт в бот» у вопроса, голосовые/кружки/стикеры не пересылаются в поддержку, повторный /start сбрасывает funnel_stage и не даёт меню, фото в анкете объекта роняет сохранение (IntegrityError на object_name=None). Результаты — в `docs/CODE_REVIEW.md`. Найдены критичные проблемы: боевой токен бота закоммичен в git (.env в истории), поиск пользователей в админке падает с 500 (`cast(str)` — подтверждено тестом), загрузка юр-документов сломана (`document_type` без `Form(...)` — подтверждено тестом), uploads пишутся мимо docker-volume, застревание в FSM анкеты объекта, «Отмена» вопроса не сбрасывает state. Плюс список несоответствий ТЗ (тексты тарифов, кнопки, 30-мин таймер, сохранение анкеты при отмене).

## Следующий шаг

- **Фаза 3 — тестирование e2e, реальный контент, деплой.** Весь код Фазы 2.5 готов и статически проверен (compileall=0), но НИ РАЗУ не гонялся вживую против Telegram/БД. Порядок:
  1. **Ручной Блок 0 (пользователь):** отозвать токен BotFather, вычистить .env из git-истории, сменить пароли, `git rm --cached __pycache__/*.pyc/.mov`, description в BotFather, удалить мусорные медиа (I6).
  2. **Локальный прогон:** поднять docker-compose (порты 127.0.0.1:5000/5433), проверить старт (admin создаёт таблицы → bot ловит через wait_for_db), пройти воронку в боте от /start до тарифов, проверить поддержку (тема, ответ админа), анкету объекта (Отмена/Отправить), рассылку (МСК-время), назначение объекта из админки.
  3. **Реальный контент:** загрузить юр-документы через админку, задать ссылки уроков/тарифов в настройках, заменить видео/фото-заглушки (кэш подхватит по mtime сам).
  4. **Деплой:** выполнить `docs/migrations/2026-07-16_fixes.sql` на прод-БД (в т.ч. file_mtime), перенести uploads в volume, проверить Caddy.
- **Известные допущения (не баги):** таймер отзывов на MemoryJobStore не переживает рестарт; FSM в MemoryStorage; cookie без secure (локальный http, в проде Caddy HTTPS).
- **Параллельно пользователь (Блок 0, руками):** отозвать токен через BotFather, вычистить .env из git-истории, сменить пароли, `git rm --cached` мусора (в т.ч. `__pycache__` — .pyc сейчас в индексе git), прописать description в BotFather.

## Что сделано в этой сессии

1. **ТЗ и архитектура** — прочитал TZ.md, задал вопросы, зафиксировал решения. Стек: Python (aiogram 3 + FastAPI), PostgreSQL, Docker, Caddy. Модель данных: 9 таблиц (users, consent_log, questions, objects, mailings, mailing_logs, legal_documents, analytics_events, settings, cached_files).

2. **Инфраструктура** — docker-compose.yml (3 сервиса: db, bot, admin), Dockerfile-ы, requirements.txt, shared/ модули (config, database, models).

3. **Ядро бота** — 10 хендлеров (start, menu, course, examples, reviews, faq, question, object, consent, support), FSM для вопросов/объектов, группа поддержки (создание тем, пересылка), APScheduler.

4. **Админка** — FastAPI + Jinja2, авторизация по паролю, дашборд, пользователи (поиск/карточка), рассылки (CRUD), объекты (назначение/отклонение), юр-документы (загрузка/публичная выдача), настройки прокси.

5. **Рассылки** — бот polling БД раз в минуту, отправляет pending-рассылки.

6. **SOCKS5 прокси** — настраивается через админку (/settings), хранится в таблице settings, бот загружает при старте.

7. **Кэширование file_id** — видео/фото отправляются по file_id из БД, без повторной загрузки.

8. **Конвертация видео** — .mov → .mp4 (легче, Telegram принимает лучше). Старые .mov в .gitignore.

9. **Русификация админки** — все enum-ы переведены, эмодзи добавлены.

10. **Инлайн-меню** — главное меню теперь инлайн-кнопки в сообщении (не reply-кнопки внизу).

11. **Юр-документы** — ссылки动态ные из БД, отдаются по публичному URL /legal/{id}/download.

12. **Уведомления** — при назначении объекта пользователь получает сообщение.

## Нюансы (грабли, лимиты, особенности)

- Порты не использовать: 3000-4000 и 8000-9000.
- Домен `andrew-bot-adminka.mak-o.ru` уже настроен в DNS.
- Caddy: `andrew-bot-adminka.mak-o.ru { reverse_proxy localhost:5000 }`.
- PostgreSQL enum: при добавлении новых значений в LegalDocTypeEnum нужно выполнять `ALTER TYPE legaldoctypeenum ADD VALUE IF NOT EXISTS 'new_value';` в БД.
- Timeout прокси: `AiohttpSession(proxy=..., timeout=300)` — timeout должен быть int, не ClientTimeout.
- Файлы: .mov в .gitignore, используются .mp4.
- Ссылки на GetCourse — заглушки (getcourse.example.com).
- Ссылки на юр-документы — заглушки (#offer, #privacy, #personal_data) до загрузки через админку.
- Бот и админка общаются через общую БД.
- After clearing cached_files (DELETE FROM cached_files) — файлы загружаются заново при первом проходе.
- **Блок D:** `scheduler` и `bot` доступны хендлерам через DI aiogram (`dp['scheduler']`/`dp['bot']`) — объявляешь имя в сигнатуре, придёт. Отложенные цепочки — только через `run_detached` (`services/delayed.py`), сессию хендлера внутрь НЕ передавать (детач открывает свою короткую `async_session`).
- **Блок D — таймер отзывов:** MemoryJobStore — job `reviews_{tg_id}` не переживает рестарт бота (осознанное допущение). Идемпотентность `send_funnel_reviews` — через проверку STAGE_ORDER ≥ watched_lessons + коммит ДО отправок; узкая гонка «кнопка ровно в момент срабатывания таймера» теоретически возможна (принято по дизайну).
- **Блок D — навигация:** `unknown_command` в start.py ловит `F.text.startswith("/")` только при `StateFilter(None)` и в private — чтобы не мешать FSM-шагам (анкета/вопрос) и группе поддержки. `menu_free_materials` больше НЕ пишет `funnel_stage="free_materials_viewed"` (стадии нет в STAGE_ORDER, это откатывало воронку) — этап трогают только воронковые хендлеры через `advance_stage`.
- **Блок D — тарифы:** «Сравнить тарифы»/«Узнать тарифы»/«Подробнее о тарифах»/«Тарифы» — всё один callback `compare_tariffs`. Чтобы не зациклить, у тарифных сообщений НЕТ кнопки «Сравнить тарифы» (только Купить+Задать вопрос), финальная клавиатура `tariff_final_kb` — без «Подробнее». url тарифов — из настроек админки (`get_tariff_urls`), плейсхолдеры getcourse.example.com до заполнения.
