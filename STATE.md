# STATE.md — состояние работы (обновляется каждой сессией)

## Текущая точка

- **2025-07-15. Фаза 2 — весь функционал реализован.** Бот: воронка, FSM, группа поддержки, рассылки (polling БД), уведомления о назначении объектов, динамические ссылки из БД. Админка: дашборд, пользователи (поиск/карточка), рассылки (CRUD), объекты (назначение/отклонение), юр-документы (загрузка/публичная выдача). Shared: 8 таблиц + free_lessons тип.

## Следующий шаг

- Тестирование: запустить docker-compose, проверить бота и админку
- Деплой на сервер: docker-compose up -d, настроить Caddy
- Дошлифовка: заменить заглушки на реальные ссылки (GetCourse, юр-документы)

## Нюансы (грабли, лимиты, особенности — пополняется по ходу)

- Обвязка переносимая: пути нигде не захардкожены.
- Stop-хук блокирует завершение ответа, если файлы проекта изменены позже STATE.md.
- Порты не использовать: 3000-4000 и 8000-9000.
- Домен `andrew-bot-adminka.mak-o.ru` уже настроен в DNS.
- Caddy: добавить `andrew-bot-adminka.mak-o.ru { reverse_proxy localhost:5000 }`.
- Файлы в `files/` — тестовые. Реальные загружаются через админку.
- Токен бота и пароли в `.env` — не коммитить.
- Юр-документы: загружаются через админку, отдаются по публичному URL `/legal/{id}/download`.
- Бесплатные уроки: ссылка хранится как LegalDocument типа free_lessons.
- Рассылки: бот проверяет БД раз в минуту, отправляет pending-рассылки.
- Уведомления: бот проверяет новые assigned-объекты, отправляет уведомления.
- GetCourse: пока заглушки, интеграция позже.
